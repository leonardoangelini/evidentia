node_modules/
.output/
.wxt/
dist/
*.log
.DS_Store
stats.html

# Configurazione locale: client ID OAuth, chiave che fissa l'id dell'estensione.
.env
.env.*
!.env.example

# Credenziali di pubblicazione: non devono mai entrare nel repository.
# (Chiave del service account del Chrome Web Store, chiavi generate in locale.)
*.pem
*.p12
sa.json
service-account*.json
key.pem
# Google Cloud nomina la chiave scaricata <progetto>-<id chiave>.json, che i
# pattern qui sopra non coprono: è finita in staging una volta, la push
# protection di GitHub l'ha fermata sul filo.
evidentia-*.json

# Export ed esperimenti locali: possono contenere documenti reali.
demo-exports/
*.evidentia.zip
